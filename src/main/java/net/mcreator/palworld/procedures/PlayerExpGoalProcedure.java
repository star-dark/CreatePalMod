package net.mcreator.palworld.procedures;

import net.neoforged.bus.api.Event;

public class PlayerExpGoalProcedure {
public static String execute(
) {
return
new java.text.DecimalFormat("##").format(Math.round(*Math.pow(1.1,)*100));
}
}
